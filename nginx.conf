events {
}

http {
    server {
        listen 80;
        location /erro {
            return 500 "Erro 500: Erro proposital!";
        }

        location / {
            return 200 "Olá! Servidor Nginx funcionando.";
        }
    }
}